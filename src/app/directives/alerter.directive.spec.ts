import { AlerterDirective } from './alerter.directive';

describe('AlerterDirective', () => {
  it('should create an instance', () => {
    const directive = new AlerterDirective();
    expect(directive).toBeTruthy();
  });
});
